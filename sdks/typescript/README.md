# TypeScript SDK — Deprecated Namespace

> **Canonical location**: [`sdk/`](../../sdk/)
>
> The primary TypeScript SDK lives at the repository root under `sdk/`. This
> `sdks/typescript/` directory is kept only for namespace clarity alongside
> `sdks/python/`.
>
> Do **not** add new TypeScript SDK code here — use `sdk/` instead.
>
> This directory intentionally contains no source code.
