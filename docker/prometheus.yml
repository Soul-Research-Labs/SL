global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "relayer"
    static_configs:
      - targets: ["relayer:9090"]

  - job_name: "lumora"
    static_configs:
      - targets: ["lumora:8080"]
